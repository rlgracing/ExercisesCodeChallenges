package com.rlg.challenges;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Memory {

	public static void main(String[] args) {

		int cap = 20;
		List<List<Integer>> fore = Arrays.asList(Arrays.asList(1,8), Arrays.asList(2,7), Arrays.asList(3,14));
		List<List<Integer>> back = Arrays.asList(Arrays.asList(1,5), Arrays.asList(2,10), Arrays.asList(3,14));
		
		List<List<Integer>> result = new ArrayList<List<Integer>>();

	}

}
